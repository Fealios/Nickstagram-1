
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model Nickstagram.ViewModels.UsersPageViewModel

@foreach(var user in Model.Users)
{
    if(@user.Id != Model.CurrentUserId)
    {
        @Html.ActionLink(user.UserName, "GuestPage", "UserPage", new { id = user.Id })
    }
    <br />

}